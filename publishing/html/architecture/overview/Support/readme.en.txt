The Dynamic HTML Standard Edition template produces HTML that
uses a cascading style sheet (CSS) file and is compliant with the
HTML version 4.0 standard. Dynamic HTML Standard Edition output
requires a version 4 or later browser.

For more information on using and customizing projects created
from this template, please see the Template Reference section in
the online help or supplied PDF files.

